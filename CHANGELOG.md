# 更新记录

## 未发布

## 0.1.11 — 2026-07-27

这一修复版本减少清理、扫描和设置流程中的阻塞，并让 CoreRobin 在关闭主窗口或切换页面后继续完成用户已经开启的监控任务。

### 修复与改进

- 清理篮执行删除时不再预先重复核对整批路径；存在的内容会继续处理，已经不存在或暂时不可访问的内容会跳过并在结果中说明，不再阻止其余项目。
- 关闭主窗口后，系统资源采样、提醒规则、历史快照与网络连接历史会继续运行；重新打开窗口时可以看到连续数据。
- 文件洞察扫描会跨页面继续执行并保留进度；空间清理结果可直接进入对应路径、查看大文件或加入清理篮，减少重复扫描和绕路操作。
- 精简移到废纸篓、永久删除、结束进程与启动项操作的确认流程；永久删除仍保留不可恢复确认，其他可恢复操作不再重复要求用户确认同一件事。
- 重新整理设置页为清晰的分类入口，加入每日缓存的更新检查，并为系统设置失败、应用关注目标暂未运行或已经移除等状态提供明确恢复操作。
- 网络质量与连接历史在页面切换后保留本次会话数据；日常模式会展示全部待处理项目，并提供直达网络详情的入口。
- 修复默认 Dock 可见性、历史空面板、首次进入时缺少默认内容，以及若干失败后无法直接重试的问题。

### 发布与质量保障

- 正式发布时会把本版本的中英文更新记录自动同步到公开网站，避免安装包已经发布而官网版本记录仍然滞后。

## 0.1.10 — 2026-07-24

这一修复版本集中改善空间清理与应用卸载体验，减少重复扫描和确认阻塞，并修复 macOS 主窗口重新打开的问题。

### 修复与改进

- 清理检查窗口不再因停留超过 60 秒而强制失效；在移到废纸篓和直接删除之间切换时会复用已经核对的文件信息，同时仍在真正处理前重新验证每个路径。
- “较小对象”不再显示为受保护项目；点击后会在受控节点预算内展开当前目录中的真实文件和文件夹，用户可以逐项检查并加入清理篮。
- 空间清理完整扫描会顺手保留一批大目录的下一层明细；之后按需展开的目录也会持久缓存，再次进入时先立即展示，并仅在路径状态变化后于后台刷新。
- 修复前后端清理扫描缓存版本不一致导致已保存扫描结果无法恢复的问题。
- 没有可验证 Bundle ID 的 macOS 应用现在仍可安全卸载应用本体；CoreRobin 会明确提示仅处理经过路径与结构验证的 `.app`，不会按名称猜测残留文件。
- 应用移到废纸篓或永久删除后会立即在原列表中显示对应状态，不再为每次卸载重新读取完整应用清单，并提供更清晰的处理进度与结果反馈。
- 修复关闭桌面主窗口后点击 macOS Dock 图标只能激活应用、无法重新显示主窗口的问题。

## 0.1.9 — 2026-07-21

这一版本集中提升日常操作体验、网络质量观察能力与发布可靠性，让清理、设置和专业监控在首次进入时更完整、更易理解。

### 新增与改进

- 网络质量页面进入后自动检查，并在页面停留期间每 30 秒轻量采样，展示最近 15 分钟的延迟、抖动、丢包趋势以及平均值、峰值和异常次数；仍保留立即复测入口。
- 清理页面在尚未扫描时改为带动态扫描视觉、范围说明和主操作按钮的完整空状态，不再展示容易误解的旧结果占位。
- 专业模式会默认选择适合检查的高负载进程，避免右侧进程详情在首次进入时保持空白。
- 统一语言选择与日常/专业模式按钮的高度和文字规格，并重新整理设置卡片布局，重做应用关注规则的输入、指标、阈值和持续时间控件。
- 清理操作无法安全打开系统废纸篓时提供更清晰的恢复路径，同时继续保留直接删除前的明确确认边界。

### 修复与性能

- 将非首屏诊断组件和清理、网络页面样式延迟加载，降低首页初始 JavaScript 与 CSS 体积，并为后续功能增长更新可审计的包体预算。
- 修复新增界面功能导致前端 Bundle 门禁越界的问题，继续保留首页 CSS 和全部 JavaScript 总量限制。

### 发布与质量保障

- 新增无 Tag 的四平台 Release Candidate 门禁，只有 Linux、Windows、Apple Silicon Mac 与 Intel Mac 的构建和签名检查全部成功并通过受保护环境审批后，才创建不可变版本 Tag。
- 修复异步 Finalize 对 Preview 的认证读取、Tauri 未压缩更新包格式及旧构建缺失 updater 签名的恢复流程，减少 Apple 公证完成后的发布中断。

## 0.1.8 — 2026-07-20

这一版本新增彻底卸载助手，并统一完善各功能中的应用名称、图标与应用清单缓存体验。

### 新增与改进

- 新增彻底卸载助手，识别应用本体及由 Bundle ID 明确归属的残留文件，并在用户确认后处理选中路径。
- 应用清单优先显示与用户语言偏好匹配的本地化名称，并在卸载助手、概览、存储、启动项等页面复用真实应用图标。
- 应用清单加入持久化缓存、指纹校验和后台刷新，减少重复扫描等待，同时在首次读取时提供明确的加载状态。

### 修复

- 修复跨平台 CI 中误编译 macOS 专属卸载与缓存代码的问题。
- 在 Tauri 发布配置中显式启用 macOS Hardened Runtime，修复签名校验器错误要求 `flags=` 位于行首的问题，并增强失败诊断。

## 0.1.7 — 2026-07-20

这一候选版本未通过发布来源校验：`src-tauri/tauri.conf.json` 仍为 0.1.6，与其余版本文件不一致。工作流在构建前终止，没有创建安装包或公开 Preview；由 0.1.8 替代发布。

## 0.1.6 — 2026-07-20

这一候选版本完成发布前置检查并提交了 macOS 公证，但签名校验器未能识别 `CodeDirectory` 行中的 Hardened Runtime 标志，因此没有创建公开 Preview；由 0.1.7 替代发布。

## 0.1.5 — 2026-07-19

这一版本集中完善专业监控与文件清理体验，新增网络、应用、启动项、文件和能耗洞察，并补齐安全清理与可恢复的重复文件工作流。

### 新增与改进

- 新增网络质量检查，统一展示联网状态、DNS、延迟、抖动与丢包，并扩展连接历史及应用、域名聚合。
- 新增应用关注列表与自定义提醒规则，以及更贴近真实使用情况的开机影响测量。
- 新增独立的“重复与长期未修改文件”工作区，支持结果缓存、状态提示、重复组选择与批量处理。
- 新增 GPU 活动和应用相对能耗影响视图，并压缩平台能力信息的视觉占用。
- 重做常驻清理篮，支持拖拽文件或目录、移入废纸篓或直接删除，并在扫描结果出现时提供明确的拖放反馈。
- 文件清理加入系统重要路径和受保护对象拦截，危险对象无法进入删除流程，并显示区别于普通拖放的保护反馈。
- 状态栏面板和 Robin 伙伴会保留用户当前的日常或专业模式，并让需要关注的提示可以直接唤起对应首页。

### 修复

- 修复空间扇形图旁目录列表因 hover 改变高度而反复闪烁的问题，并允许目录条目直接拖入清理篮。
- 修复重复文件结果区域无法滚动、短列表吞掉滚轮事件，以及可回收空间图标未居中的样式问题。
- 统一日常与专业模式切换按钮顺序，修正新增功能的布局、滚动和高密度结果展示。

### 发布与质量保障

- macOS Release 默认改为在发布者 Mac 上完成双架构 Developer ID 签名、公证与校验，再由 GitHub Actions 导入带 tag、commit 和 SHA-256 约束的资产；Windows/Linux 继续使用 GitHub-hosted runner，只有手动明确选择时才启用 GitHub-hosted macOS 构建。
- GitHub-hosted macOS 回退构建为 Apple 公证队列预留最长四小时，并按 submission ID 容错轮询；短暂断网只会重试状态查询，不会重复上传 DMG 或中断发布。

## 0.1.4 — 2026-07-19

这一版本已完成内部构建与 Windows、Linux 产物验证，但 Apple 公证提交持续排队超过两小时，且一次状态查询遇到临时网络中断，因此没有创建公开 Release；由 0.1.5 替代发布。

## 0.1.3 — 2026-07-19

这一版本已完成内部构建和全平台验证，但 GitHub-hosted macOS 公证在 Apple 队列中超过任务时限，因此没有创建公开 Release；由 0.1.4 替代发布。

## 0.1.2 — 2026-07-18

这一版本替代仅进入内部 staging、未公开发布的 0.1.1，保留应用内可信更新与 macOS Developer ID 分发能力，并修正首次 Apple 公证构建暴露出的安装包链路问题。

### 修复

- macOS Release 同时构建 `app` 与 `dmg` 目标，确保 Apple Silicon 与 Intel 包都包含应用内更新所需的 `.app.tar.gz` 和签名文件。
- 对完成 Developer ID 签名的最终 DMG 显式执行 Apple 公证并装订票据，验证通过后再上传发布资产，避免只公证内部 App 而遗漏外层安装镜像。

## 0.1.1 — 2026-07-18

这一版本完成应用内可信更新与 macOS Developer ID 分发链路，让 CoreRobin 的公开预览包可以通过 Apple 签名、公证和 Gatekeeper 验证。

### 新增与改进

- “关于与支持”现在可在应用内检查、下载并验签公开更新，安装完成后由用户明确选择重新启动；浏览器演示模式继续只提供公开 Release 下载入口。
- Release 流程为 macOS Apple Silicon、macOS Intel、Windows x64 和 Linux x64 自动生成独立命名的签名更新包与 `latest.json`，缺少任一包或签名时停止发布。
- macOS Release 改用 Developer ID Application 对应用和 DMG 签名，通过 App Store Connect API 完成 Apple 公证并装订票据；发布门禁会同时验证 Hardened Runtime、开发团队、Gatekeeper 和公证票据。

### 修复

- 修复实机冒烟与性能基线脚本无法接受 pnpm 标准参数分隔符的问题，并让 CI 按文档中的真实调用方式验证这两项工具。

## 0.1.0 — 2026-07-17

这是 CoreRobin 的首个完整公开预览版本，补齐首次使用、更新发现、支持反馈和可信发布门禁，并确定正式应用标识。

### 新增与改进

- 新增三步首次使用引导，说明日常模式与专业模式、后台状态栏行为，以及通知、Robin 伙伴和完整磁盘访问等可选能力。
- 在日常模式与专业模式设置中加入统一的“关于与支持”，集中展示版本、平台和架构，支持检查更新、查看版本记录、打开指南与隐私说明、报告问题和复制脱敏诊断摘要。
- 增加两步确认的本机数据清除操作；保留系统管理的登录启动与隐私权限，清除历史、提醒、操作记录、WebView 数据和原生扫描缓存。
- 温度与电池读数区分等待首个样本、系统未提供和暂时读取失败，并展示原因和最后成功采样时间。
- 专业模式采样间隔改为“低功耗、平衡、实时”预设，同时保留高级精确间隔设置。
- macOS 增加标准设置快捷键与状态栏“设置 / 关于 CoreRobin”入口。
- 正式应用标识调整为 `com.corerobin.monitor`；当前没有需要迁移的已安装用户数据。
- Internal 仓库不再维护重复官网，公开站点和用户文档统一由 `corerobin-monitor` 承载，并通过结构化源文件哈希检查同步状态。

### 发布与质量保障

- Release 构建增加 macOS DMG、Windows MSI/NSIS、Linux DEB/AppImage 的归档结构、目标架构、版本和应用标识检查。
- 主窗口、状态栏面板和 Robin 伙伴新增共享健康状态、主题与语言同步契约测试。
- 建立 WebView 入口与包体预算、后台采样性能契约，以及固定机器性能基线记录工具。
- 发布流程改为两阶段：tag 构建只创建公开 Draft，只有四类目标设备的真实安装冒烟证据全部通过后，才能提升为 latest 并更新官网下载清单。
- 公开站点增加 390、768、1440 三档布局检查、axe 无障碍检查、移动导航焦点、语言路由、系统推荐与 Release 资产可用性验证。

### 平台说明

- macOS 安装包具备完整的 ad-hoc 签名，但仍未配置 Developer ID 签名或 Apple 公证；首次打开可能仍需在“系统设置 → 隐私与安全性”中确认。
- Windows 与 Linux 安装包继续作为早期预览版本提供，尚未配置平台发布签名。
- 正式发布前必须分别完成 macOS Apple Silicon、macOS Intel、Windows x64 和 Linux x64 的真实设备验证。

## 0.0.4 — 2026-07-17

这是一个针对 macOS 安装可靠性与空间扫描体验的修复版本。

### 修复与改进

- 修复 Apple Silicon DMG 中应用包签名不完整、可能被 macOS 判定为“已损坏”的问题。
- macOS 安装包改为对完整应用包进行 ad-hoc 签名，并在发布前从 DMG 重新挂载验证签名、资源封装和目标架构。
- 恢复空间扫描时的彩色轨道、扫描扇区、呼吸光晕和节点脉冲动画，同时保留实时扫描位置、数量与已发现空间。
- 调整状态栏缩略面板高度，并增加小屏幕滚动兜底，避免新增操作区被窗口边界截断。
- Apple Silicon Mac 即使临时运行 Intel 包，也会改用原生系统查询补齐电池健康度；推荐安装原生 Apple Silicon 包，以正常读取温度传感器。
- 下载页明确区分操作系统检测与芯片选择，并补充“无法验证”与“应用已损坏”两类 macOS 提示的处理方式。

### 平台说明

- macOS 安装包具备完整的 ad-hoc 签名，但仍未配置 Developer ID 签名或 Apple 公证；首次打开可能仍需在“系统设置 → 隐私与安全性”中确认。
- Windows 与 Linux 安装包继续作为早期预览版本提供，尚未配置平台发布签名。

## 0.0.3 — 2026-07-17

这一版本完成 CoreRobin 品牌与公开分发链路升级，并让主窗口、状态栏面板和 Robin 桌面伙伴在日常诊断与操作记录上保持一致。

### 新增与改进

- 全面更新 CoreRobin 与 Robin 的应用图标、动态形象、官网和产品截图。
- 主窗口、状态栏面板和 Robin 桌面伙伴共用后台健康状态，减少窗口切换或重新打开后的信息丢失。
- 优化日常问题的稳定判定与恢复确认，避免短暂波动导致问题数量频繁跳变。
- 空间清理支持全磁盘扫描权限引导，并保留无需授权即可扫描可访问区域的清晰入口。
- 增加用户操作记录、进程重启、路径操作和启动项管理能力，同时保留操作前复核与失败关闭策略。
- 完善私有源码仓库到公开 Release 仓库的跨平台构建、校验、SBOM 与 Sigstore 分发流程。
- 修正 Windows runner 上清理取消测试对固定进度节奏的错误依赖。

### 平台说明

- macOS 安装包仍未配置 Developer ID 签名或 Apple 公证。
- Windows 与 Linux 安装包继续作为早期预览版本提供，尚未配置平台发布签名。

## 0.0.2 — 2026-07-16

这一版本让日常助手、后台状态和多语言界面能够作为一个完整体验协同工作，并进一步收紧清理与进程操作的安全边界。

### 新增与改进

- 日常模式使用稳定的事件生命周期确认持续问题、等待稳定恢复，并在记录中把提醒和恢复合并为一次完整事件。
- 主窗口、状态栏面板和 Robin 桌面伙伴共享同一份版本化健康状态；关闭主窗口后仍能在后台保持一致结论。
- 增加登录时静默启动、macOS Dock 图标显示控制，以及更接近原生菜单栏体验的状态面板与双击打开主窗口操作。
- 界面新增繁體中文、日本語、Deutsch、Français、Español、Português (Brasil)、한국어和Русский，连同简体中文与 English 共支持 10 种语言。
- 国际化资源按窗口和 namespace 动态加载，并增加键、插值、复数形式及产品术语的自动检查。
- 清理扫描、缓存和永久删除改用更严格的能力边界与文件句柄复核；目标变化时会使旧确认失效。
- 优化隐藏窗口采样、连接排序、清理扫描协调和扇形图绘制，减少后台与大目录场景的资源占用。
- Release 流程增加受信分支与版本校验、跨平台安装包、SHA-256、SPDX SBOM 和来源证明。

### 平台说明

- macOS 已完成真实设备运行与原生打包验证；安装包仍未配置 Developer ID 签名或 Apple 公证。
- Windows 与 Linux 安装包继续作为早期预览版本提供，尚未配置平台发布签名。

## 0.0.1 — 2026-07-15

CoreRobin 的首个公开版本。

### 主要功能

- 集中展示 CPU、内存、磁盘、网络和最近五分钟的变化趋势。
- 用简单模式快速判断电脑状态，用专业模式查看进程树、命令行、磁盘 I/O 和活动连接。
- 提供存储概览、启动项管理、本机历史记录、桌面提醒和状态栏面板。
- 完整扫描个人文件，用 Canvas 扇形图展示空间去向，并支持逐层查看。
- 通过清理篮集中核对待删除内容，永久删除前会复核路径、类型和文件变化。
- 支持简体中文与英文界面，并提供网页版和仓库内使用指南。

### 平台说明

- macOS 已完成真实设备运行与原生打包验证；首个版本尚未经过 Apple 公证。
- Windows 与 Linux 安装包由对应系统的 GitHub Actions 构建，目前作为早期预览版本提供。
